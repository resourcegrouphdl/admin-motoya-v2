export interface ChecklistDNI {
  readonly textoLegible: boolean;
  readonly fotoClara: boolean;
  readonly datosCompletos: boolean;
  readonly documentoIntegro: boolean;
  readonly sinAlteraciones: boolean;
  readonly vigente: boolean;
}
