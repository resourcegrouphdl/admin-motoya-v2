export interface UserModelResponse {
            uid: string,
            email: string,
            displayName:string,
            token:string,
            providerId: string,
            refreshToken: string,
            emailVerified: string
}